#include <stdio.h>
/**
 * main - Entry point
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */
int main(void)
{
fprintf("Size of a char: %d bytes(s)\n", sizeof(char));
fprintf("Size of an int: %d bytes(s)\n", sizeof(int));
fprintf("Size of a long int: %d bytes(s)\n", sizeof(long int));
fprintf("Size of a long long int: %d bytes(s)\n", sizeof(long long int));
fprintf("Size of a float: %d bytes(s)\n", sizeof(float));
/* returns 0 */
return (0);
}
