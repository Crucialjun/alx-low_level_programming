#include <stdio.h>
#include <string.h>
/**
 * _strcpy - check the code
 *
 * @dest : char pointer
 * @src : char pointer
 *
 * Return: length of s.
 */
char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}
